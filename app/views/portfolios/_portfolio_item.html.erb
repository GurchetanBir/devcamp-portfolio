


<tr>
  <td><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></td> <br> 
  <td><%= portfolio_item.subtitle %></td>   <br>
  <td><%= portfolio_item.body%></td>   <br>
  <td><%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %></td>   <br>
  <td><%= link_to 'Edit', edit_portfolio_path(portfolio_item) %></td>     <br>
  <td><%= button_to "Destroy this Portfolio", portfolio_item, method: :delete, data: {confirm: "Are you sure?"} %>    </td> <br>

  
</tr>  